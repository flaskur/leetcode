/**
 * In a binary tree, the root is depth 0. Two nodes are cousins in a binary tree if they have the same depth but different parents.
 * Find the depth of each node individually by BFS, and keep track of parent?
 * BST so you can traverse through tree until you reach the value or node is null.
 * 
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 * 
 * @param {TreeNode} root
 * @param {number} x
 * @param {number} y
 * @return {boolean}
 */
const isCousins = (root, x, y) => {};
